<%= form_for(@bookmark_manager) do |f| %>
  <% if @bookmark_manager.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bookmark_manager.errors.count, "error") %> prohibited this bookmark_manager from being saved:</h2>

      <ul>
      <% @bookmark_manager.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
<div class="field">
  <%= f.label :tag_list, "Tags (separated by commas)" %><br />
  <%= f.text_field :tag_list %>
</div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
